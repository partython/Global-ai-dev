# Migration runner service
# This directory exists so Docker can COPY it into the container.
# The actual migration files are in shared/migrations/alembic/versions/
# Run with: python -m alembic -c shared/migrations/alembic.ini upgrade head
